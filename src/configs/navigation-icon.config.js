import { HiCog, HiPuzzle, HiTemplate, HiUsers } from "react-icons/hi";

const navigationIcon = {
  dashboard: <HiTemplate />,
  operation: <HiCog />,
  client: <HiUsers />,
  config: <HiPuzzle />,
};

export default navigationIcon;
